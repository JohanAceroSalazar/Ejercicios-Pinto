public class exercise22 {

public static Principal pr = new Principal();

    public static void suma() {
        pr.calcular = 0;

        for(pr.contador = 1; pr.contador <= pr.b; pr.contador++){
            pr.calcular = pr.calcular + pr.a;
        }
        System.out.println("Producto: " + pr.calcular);
    }
}